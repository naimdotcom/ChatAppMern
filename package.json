{
  "name": "chatappmern",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && cd frontend && npm install && npm install -D @vitejs/plugin-react && npm run build && cd ../backend && npm run build",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
