{
  "name": "chatappmern",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && rm -rf node_modules && npm install && NODE_ENV=production npx vite build && cd ../backend && npm run build",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
